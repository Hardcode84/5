// SPDX-FileCopyrightText: 2026 hc contributors
//
// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

#ifndef HC_FRONT_TRANSFORMS_PASSES_H
#define HC_FRONT_TRANSFORMS_PASSES_H

#include "mlir/Pass/Pass.h"

#include <memory>

namespace mlir::hc::front {

// ODS-generated pass-base declarations plus the `createXxxPass()`
// factories (one per pass in `Passes.td`). With no `let constructor`
// in `Passes.td`, tablegen emits each factory as a friend of its
// CRTP base so the DerivedT is the one we `std::make_unique<>` here
// — see `Passes.td` for the per-pass contracts.
#define GEN_PASS_DECL
#include "hc/Front/Transforms/Passes.h.inc"

// `registerHCFrontTransformsPasses()` — generated by tablegen from
// `Passes.td`; registers every pass declared there (currently
// `-hc-front-inline`) with the global `mlir-opt`-style registry.
#define GEN_PASS_REGISTRATION
#include "hc/Front/Transforms/Passes.h.inc"

} // namespace mlir::hc::front

#endif // HC_FRONT_TRANSFORMS_PASSES_H
